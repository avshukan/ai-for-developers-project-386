# Dependencies
node_modules/

# Go backend build output
backend/bin/

# TypeSpec default emitter output (this project emits to openapi/ instead)
tsp-output/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Environment files
.env
.env.*
!.env.example

# Local MCP override (may hold secrets; never commit). The committed
# .mcp.json holds no secret — it reads the key from ${RENDER_API_KEY}.
.mcp.local.json
