# Base URL of the Call Booking API.
# In development this points at the Prism mock server (see `make api-mock`).
# Copy this file to `.env` to override locally.
VITE_API_BASE_URL=http://localhost:4010
