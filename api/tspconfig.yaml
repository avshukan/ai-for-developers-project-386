emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    # {project-root} is the api/ dir (where this config lives), so go up one
    # level to emit into the repo-root openapi/ directory.
    emitter-output-dir: "{project-root}/../openapi"
    output-file: "openapi.yaml"
