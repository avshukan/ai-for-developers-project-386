{
  "name": "call-booking-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright integration tests that drive the Vue SPA against the real Go backend over the public API contract. See docs/adr/0003-integration-tests-playwright.md.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
}
